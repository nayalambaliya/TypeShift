name: Release Android APK

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag_name:
        description: 'Release tag (e.g. v1.3)'
        required: true
        default: 'v1.3'

permissions:
  contents: write

jobs:
  build-android:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Build APK
        working-directory: android
        run: ./gradlew assembleDebug

      - name: Rename APK
        run: mv android/app/build/outputs/apk/debug/app-debug.apk TypeShift-android.apk

      - name: Upload to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.inputs.tag_name || github.ref_name }}
          files: TypeShift-android.apk
          body: |
            ## Installation (Android)
            1. Download **TypeShift-android.apk** below
            2. On your phone: **Settings → Apps → Install unknown apps** → allow your browser or Files app
            3. Open the downloaded APK and tap **Install**
            4. Open TypeShift → enter your free [Groq API key](https://console.groq.com) → enable the Accessibility Service
